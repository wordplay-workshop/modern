# Site settings
title: "Wordplay @ ACL 2024"
description: > # this means to ignore newlines until "baseurl:"
  Official website for the Wordplay Workshop at ACL 2024.
baseurl: "/modern/" # the subpath of your site, e.g. /blog/
url: "" # the base hostname & protocol for your site
# author: Copyright &copy; 2019 - Start Bootstrap
lang: en_US

# Build settings
safe: true # always trues on github-pages, disables _plugins/
markdown: kramdown
sass:
  indentWidth: 4
  style: compressed
  precision: 10
plugins:
  - jekyll-sitemap
  - jekyll-seo-tag
exclude:
  - Gemfile
  - Gemfile.lock
  - build-in-docker.sh
  - README.md
